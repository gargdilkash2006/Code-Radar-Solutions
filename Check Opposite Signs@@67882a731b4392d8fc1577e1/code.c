#include <stdio.h>
int main() {
    int s , r ;
    scanf("%d",&s);
    scanf("%d",&r);
    if (s>0 && r<o) {
        printf("True");
    }
    else {
        printf("False");
    }
    return 0;
}