#include <stdio.h>
int main() {
    int b , c;
    scanf("%d",&b);
    scanf("%d",&c);
    if(b>0 && c>0) {
        printf("True");
    }
    else {
        printf("False");
    }
    return 0;
}