#include <stdio.h>
int main() {
    int x , y;
    scanf("%d",&x);
    scanf("%d",&y);
    if(a>0 || b>0) {
        printf("True");
    }
    else {
        printf("False");
    }
    return 0;
}